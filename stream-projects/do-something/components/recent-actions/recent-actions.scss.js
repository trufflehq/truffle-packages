import { scss } from "../../deps.ts";

export default scss`
  .c-recent-actions {
    .header {
      font-style: normal;
      font-weight: 600;
      font-size: 14px;
      letter-spacing: 0.0025em;
      text-transform: uppercase;
      color: rgba(255, 255, 255, 0.7);
      margin-bottom: 20px;
    }
  }
`;
